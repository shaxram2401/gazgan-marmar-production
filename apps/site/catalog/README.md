# Catalog folder

Place the export catalog PDF here:

```
Gazgan-Marmo-Export-Catalog-2026.pdf
```

The download button on the website points to:
`/catalog/Gazgan-Marmo-Export-Catalog-2026.pdf`

To change the filename, update `GAZGAN_CONFIG.catalogUrl` in `index.html`.
